version: 2
project:
  name: {{PROJECT_NAME_JSON}}
  root: "."
workspaces: {{WORKSPACES_JSON}}
tracking:
  contracts: {{CONTRACT_TRACKING_JSON}}
guards:
  enabled: true
  critical_actions: {{CRITICAL_ACTIONS_JSON}}
features: {{FEATURES_JSON}}
extensions: {}
documents: {{DOCUMENTS_JSON}}
exclude: {{EXCLUDE_JSON}}
